extend type Query {
  threads(userId: Int!): [Thread!]!
  threadById(id: Int!): Thread
}

type Thread {
  id: Int!
  last_message_at: String!
  thread_read: Boolean!

  users: [User!]!
  messages: [Message]!
  lastMessage: Message!
}
