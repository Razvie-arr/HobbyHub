type EventType {
  id: Int!
  name: String!
  category: String!
}

extend type Query {
  eventTypes(offset: Int, limit: Int): [EventType!]!
  eventTypeById(id: Int!): EventType
  eventTypesByIds(ids: [Int!]!): [EventType!]!
}
